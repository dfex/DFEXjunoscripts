var $count = 1;
match / {
    call update-count($myparam = $count);
}
template update-count($myparam) {
    expr $count _ ", " $myparam _"\n";
    if ($myparam != 4) {
        call update-count($myparam = $myparam + 1)
    }
}

1, 1
1, 2
1, 3
1, 4
1, 5



var $active = call sum-active($active = 0, $neighbour-list);

template sum-active($active, $neighbour-list) {
	for-each ($neighbour-list/bgp-peer) {